const qiteDexAbi = require("./qite-dex-abi.json")
const qiteAddresses = require("./qite-addresses.json")
const qitePool = require("./qite-pool-abi.json")
const qitePoolToken = require("./qite-pool-token-abi.json")

module.exports = {
    qiteDexAbi,
    qiteAddresses,
    qitePool,
    qitePoolToken,
}
